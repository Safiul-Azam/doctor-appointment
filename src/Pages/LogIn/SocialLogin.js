import React from 'react';

const SocialLogin = () => {
    return (
        <div>
            <div class="divider text-accent">OR</div>
            <button className='w-full text-text-accent text-lg font-semibold border-4 p-3 rounded-xl'>Google sign in</button>
        </div>
    );
};

export default SocialLogin;